apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- github.com/open-policy-agent/gatekeeper-library/library  # OPA Gatekeeper - apply all template
- constraints.yaml # apply the actual constrains using the templates above

helmCharts:
- name: kyverno  # GOTCHA - this is an umbrella chart (empty chart with links to other charts via dependencies), need to explicitly use "helm" instead of "kustomize with helm inflator"
 